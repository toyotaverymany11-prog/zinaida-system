# ПРОТОКОЛ ДОСТАВКИ КАРТИНОК ОЛЕГУ
## 12.07.2026 — после 3-кратной ошибки «та же картинка»

### ЖЕЛЕЗНЫЕ ПРАВИЛА
1. **Никогда не копировать имена старых файлов.** Если файл уже назывался `/tmp/slide1_done.png` — это имя ИСПОРЧЕНО. Следующую генерацию сохранять под НОВЫМ именем.
2. **Перед показом: `rm -f /tmp/slide*` + скачать заново.** Удалить все старые слайды, затем скачать свежий URL.
3. **Проверить свежесть:** `ls -lt /tmp/*.png | head` — дата создания должна быть только что.
4. **Проверить размер:** файл > 0 байт, не пустой.
5. **Проверить лицо:** если на картинке человек — vision_analyze с проверкой по эталону ZINAIDA_FACE_REFERENCE.md.
6. **Проверить кириллицу:** vision_analyze побуквенно, все ли буквы кириллица.
7. **Если ошибка — НЕ ПОКАЗЫВАТЬ.** Исправить, перегенерировать, перепроверить.
8. **Показать один раз.** Если Олег не увидел/не открыл — переслать через другую ссылку, не повторять тот же файл.
